%!TEX options=-shell-escape
%!TEX program=pdflatex

% Global font configuration
% in order to load memoir!
% \newcommand*{\memfontpack}{XCharter}
\newcommand*{\memfontpack}{tempora}
\input{config.tex}
\documentclass[
    a4paper,
    article,
    oneside,
    onecolumn,
    14pt,
    extrafontsizes,
    openright,
    \Status
]{memoir}

%Useful condition operator
\usepackage{ifthen}

%%%%%%% Change with caution! Wrong load order could wreck everything %%%%%%%
%Font configuration
\input{styles/fonts}
%Math tools
\input{styles/math}
%Text design (padding, interval e.t.c)
\input{styles/text}
%Page design configurtion
\input{styles/page}
%Media configuration (images e.t.c)
\input{styles/media}
%Table configs
\input{styles/table}
%Algorithm and listing styles
\input{styles/algorithms}

% Bibliography (GOST style)
\usepackage[% 
  backend=biber, %подключение пакета biber
  bibstyle=gost-numeric, %подключение одного из четырех главных стилей biblatex-gost 
  citestyle=numeric-comp, %подключение стиля 
  language=auto, %указание сортировки языков
  sorting=none, %тип сортировки в библиографии
  doi=true, 
  eprint=false, 
  isbn=false, 
  dashed=true, 
  url=true
]{biblatex}
\addbibresource{Lab-report.bib}

% Команды шаблона
\input{styles/commands}

% Пользовательские команды
\input{commands}


\begin{document}
    %Титульная страница
    \input{styles/title}

    %Вставляем оглавление
    \clearpage
    \tableofcontents*
    \clearpage

    %Вставляем основное содержание
    \Source

    %Печатаем библиографию
    \clearpage
    \printbibliography
\end{document}